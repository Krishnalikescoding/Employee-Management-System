# API base URL (no trailing slash)
VITE_API_URL=https://manthan-infotech-workforce.onrender.com

# Local backend only — uncomment and use with `npm run dev` in backend/
# VITE_API_URL=http://localhost:5000
