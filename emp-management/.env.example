# Local development
VITE_API_URL=http://localhost:5000

# ── Netlify (required at BUILD time) ──
# Site settings → Environment variables → add:
#   VITE_API_URL = https://your-api.onrender.com
# Then trigger a new deploy (rebuild). Vite bakes this into the JS bundle.
